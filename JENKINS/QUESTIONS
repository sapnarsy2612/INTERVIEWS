Jenkinsfile ?
what is choice parameter ?
parameters in jenkins 
INF and ENV variable differece ?
y to use parameters when we have INF 
how to integrate jenkins with sso / ldap
jenkins slaves 
What are the main types of Jenkins jobs (e.g., Freestyle, Pipeline, Multi-configuration/Matrix, Folder) and when would you choose one over another?
Explain the key differences between a Freestyle project and a Pipeline job (using a Jenkinsfile).
How do you parameterize a Jenkins job, and what are the common use cases for build parameters?
Describe the steps you would take to configure a job to pull source code from a Git repository and only build changes from a specific branch.
How do you use post-build actions or the post section in a declarative pipeline, and give an example of a common post-build task?
What is the Jenkins workspace, and how is it managed across different build runs or jobs?
Differentiate between Declarative and Scripted Pipeline syntax in Jenkins. What are the pros and cons of each?
Explain the concepts of Agent, Stage, and Step within a Jenkins Pipeline.
What is the purpose of the Jenkinsfile, and where should it be stored?
How do you implement parallel execution for tests within a Jenkins Pipeline? Provide an example of a stage structure that uses parallelism.
What are Jenkins Shared Libraries, and how do they promote code reuse and maintainability across multiple jobs?
How do you handle sensitive data like passwords or API keys (secrets) within a Pipeline job securely? (Mention the Credentials plugin or specific steps like withCredentials).
What are the various ways to trigger a Jenkins job, and when is each method appropriate? (e.g., Poll SCM, Webhooks, Build after other projects, Build periodically).
Explain the functionality and common configuration of Poll SCM vs. a Webhook integration (like with GitHub or GitLab). Which is generally preferred for a high-frequency CI environment, and why?
How do you use the cron-like syntax in Jenkins for the "Build periodically" trigger? Provide an example for scheduling a build every Monday at 3:00 AM.
How would you programmatically trigger a downstream job from a successful upstream job, and what information (e.g., build parameters) would you typically pass along?
How do you archive artifacts in a Jenkins job, and why is artifact archival an important step?
What does it mean to fingerprint a build artifact in Jenkins? Why would you use this feature?
How do you configure Jenkins to publish build results (e.g., test reports from JUnit or code coverage reports)?
Describe how you would clean up the workspace before or after a build.
A Jenkins job starts failing intermittently. What is your step-by-step troubleshooting approach?
How do you configure a Jenkins Pipeline to retry a failed step (like a flaky integration test) before marking the entire build as failed?
How can you implement an approval gate or manual intervention step in a Pipeline before proceeding to a sensitive stage like production deployment?
What is the significance of the Console Output in a Jenkins build? What key information do you look for when a build fails?
What are your best practices for keeping your Jenkinsfile clean, readable, and maintainable?
How do you configure a Jenkins job to run on a specific agent/node, and how does Jenkins select an agent when no preference is set?
Explain the concept of Global Tool Configuration in Jenkins and how jobs utilize tools defined there (e.g., Maven, JDK).
In the context of scaling, why is it considered a best practice to execute builds on agents (slaves) instead of the Jenkins controller (master)?
How have you used Jenkins to facilitate Infrastructure as Code (IaC) workflows (e.g., integrating with Terraform or Ansible)?
What are Multibranch Pipelines, and how do they automate the creation of jobs for new branches and pull requests?
What is the fundamental difference between Declarative Pipeline and Scripted Pipeline?
Why would you choose a Declarative Pipeline over a Scripted Pipeline for a new project?
In which scenarios would the flexibility of a Scripted Pipeline be absolutely necessary?
Explain the key sections/blocks of a Declarative Pipeline (e.g., pipeline, agent, stages, steps, post).
What is the role of the agent directive in a Jenkins Pipeline, and what are the different ways you can define it? (e.g., any, none, label, specifying a specific image like docker).
How do you handle parameters and environment variables within a Jenkinsfile?
How can you implement parallel execution of stages/steps in a Jenkins Pipeline?
Describe how you would implement a conditional execution of a stage (e.g., only running deployment on the main branch).
What is a Shared Library in Jenkins, and why is it beneficial for pipelines?
Explain the difference between a step and a stage in a pipeline context.
How do you manage secrets (credentials) securely within a Jenkins Pipeline?
What are the typical steps you would include in the post section of a Declarative Pipeline?
How do you handle error conditions or failed steps in a Pipeline (e.g., using try/catch in Scripted or failure block in post in Declarative)?
What is the groovy sandbox in Jenkins, and how does it relate to pipeline security?
What is a "Multibranch Pipeline," and when would you use it?
What is the primary purpose of using parameters in a Jenkins job or pipeline?
How do you enable a Jenkins job to accept parameters?
Name three common types of parameters in Jenkins and give a scenario for each.
In a Jenkins Pipeline (Jenkinsfile), which specific directive is used to define parameters?
How do you access the value of a parameter (e.g., a parameter named BRANCH_NAME) within a Declarative Pipeline step (like a shell script)?
Explain the difference between a built-in Choice Parameter and the dynamic functionality provided by the Active Choices Plugin. When would you use the latter?
You need to pass a large block of configuration data to a job. Which parameter type is most suitable, and how is it accessed inside a shell step?
How can you make the selection in one parameter field dynamically change the options available in a second parameter field? (Relates to the Active Choices Plugin's Reactive parameters).
Describe how to pass a parameter value from an upstream Jenkins job to a downstream job. (This involves using the Parameterized Trigger Plugin or similar functionality).
What is the difference between a parameter defined in the Jenkins UI configuration for a Freestyle job and a parameter defined in the parameters { ... } block of a Jenkinsfile?
Why is it a security risk to use a String Parameter for sensitive information like passwords or API keys? What is the preferred and more secure alternative in Jenkins?
In terms of best practices, what should you always consider when defining a parameter, especially for critical jobs like deployment? (Hint: Think about safety/ease of use).
How would you enforce validation on a String parameter (e.g., ensure it matches a specific format like a Git commit hash or a major.minor.patch version) before the main build steps run?
Imagine a complex pipeline with 15 parameters. How can you group or organize them for a better user experience on the "Build with Parameters" screen? (Relates to plugins like Parameter Separator or organizing logic in the Jenkinsfile).
A parameter's value needs to be used to control conditional execution of a stage. How would you implement this using the when directive in a Declarative Pipeline?
What is a POM? Explain its significance and list some of the key elements it contains.
Explain the "Convention over Configuration" principle in Maven.
What are the three build lifecycles in Maven? Name the main phases of the Default Lifecycle.
Differentiate between a Maven Phase and a Maven Goal. How are goals bound to phases?
What are Maven Repositories? Describe the three types and how Maven searches them for dependencies.
Explain the concept of transitive dependencies. How does Maven handle them?
What is Dependency Mediation?
List and explain the different Dependency Scopes in Maven (e.g., compile, provided, runtime, test, system, import).
How do you resolve a dependency conflict in Maven? What is Dependency Exclusion?
What is a Maven Snapshot? Explain the difference between a Snapshot version and a Release version.
What is a Maven Plugin? Name two common plugins and their purpose (e.g., Surefire, Compiler).
What are Maven Profiles? Why and how would you use them in a project? List ways to activate a profile.
How would you run only a specific test class or method using a Maven command?
How can you define and refer to properties in your $\text{pom.xml}$?
Explain the standard Maven project directory structure (e.g., src/main/java, src/test/java).
Explain the difference between Ant and Maven.
What is Super POM?
How can you create a multi-module project in Maven?
Why is Java a mandatory prerequisite for Jenkins?
What is the default port on which Jenkins runs after a successful installation, and how would you change it?
Where is the initialAdminPassword located after the first-time installation of Jenkins?
Explain the difference between installing Jenkins using a native system package (like apt or yum) versus running the generic .war file.
What is the primary factor that dictates the recommended hardware for a Jenkins server (Controller/Master)?
Explain the concept of the 'Jenkins Home Directory' ($JENKINS_HOME) and what critical information it stores.
When setting up a Distributed Build Architecture (Master-Agent), what is the key prerequisite for a machine to become a Jenkins Agent (Node)
Before creating a Jenkins job to build a Java application using Maven, what tool must be installed on the Jenkins server or agent?"
How does Jenkins primarily interact with a Source Code Management (SCM) system like Git?
What is the prerequisite for integrating Docker into a Jenkins pipeline
